version: 2.1

jobs:
  build:
  working_directory: ~/Employees
  docker:
    - image: circleci/node:latest  # Change this to match your project’s needs
  steps:
    - checkout
    - run:
        name: Install dependencies
        command: npm install
    - run:
        name: Run tests
        command: npm test

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
